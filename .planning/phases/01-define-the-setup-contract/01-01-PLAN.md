---
phase: 01-define-the-setup-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "The project name and purpose are explicit in the planning docs"
    - "The setup guide's scope is captured as checkable requirements"
    - "Every active requirement maps to a roadmap phase"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project framing, core value, constraints, and scope"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceable v1 requirements and out-of-scope boundaries"
    - path: ".planning/ROADMAP.md"
      provides: "Phase breakdown and requirement coverage mapping"
    - path: ".planning/STATE.md"
      provides: "Current project memory and phase reference"
  key_links:
    - from: "docs/best-setup-ever.md"
      to: ".planning/PROJECT.md"
      via: "shared project framing and audience"
      pattern: "WSL|token|setup"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement traceability"
      pattern: "DOCS-01|DOCS-02"
---

<objective>
Lock the project definition for the WSL token-optimization setup so downstream phases can execute against a stable contract.

Purpose: The repo already contains the guide and bootstrap script, but the planning layer needs a concrete, traceable definition of what this project is before any hardening work begins.
Output: Updated project framing, requirements, roadmap traceability, and phase state under `.planning/`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
@docs/best-setup-ever.md
@setup.sh
@opencode.json

<interfaces>
From `.planning/codebase/ARCHITECTURE.md`:
```text
The repository is organized as a documentation-first repo root with one skill subtree and a Bash bootstrap script. There is no application API or service layer.
```

From `.planning/codebase/STACK.md`:
```text
Primary assets are Markdown documents, Bash automation, JSON configuration, and a Python linter for the RouterOS skill.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile project framing and requirements</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>Refine the project framing so it clearly describes the WSL-first token-optimization setup, the intended user, the core value, and the hard boundaries. Ensure the requirements are atomic, user-facing, and directly traceable to the setup guide in `docs/best-setup-ever.md`, with explicit out-of-scope entries for the external tools that are only orchestrated by this repo.</action>
  <verify><automated>grep -nE 'Best AI Setup Ever|DOCS-01|DOCS-02|Out of Scope|Make the AI coding environment easy to stand up' .planning/PROJECT.md .planning/REQUIREMENTS.md</automated></verify>
  <done>The project description, core value, active requirements, and exclusions are aligned with the setup guide and the phase covers exactly the documented setup contract.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize traceability and phase state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Update the roadmap and state so Phase 1 explicitly records its plan list, requirement coverage, and current focus. Keep the phase naming stable, preserve the brownfield note, and make sure the requirement IDs are mapped to the phase without leaving any active requirement unmapped.</action>
  <verify><automated>node -e "const fs=require('fs'); const roadmap=fs.readFileSync('.planning/ROADMAP.md','utf8'); if(!roadmap.includes('01-01-PLAN.md')) process.exit(1); const state=fs.readFileSync('.planning/STATE.md','utf8'); if(!state.includes('Phase 1 - define the setup contract')) process.exit(1); console.log('traceability ok')"</automated></verify>
  <done>The roadmap shows the Phase 1 plan entry, the state file reflects the current focus, and the phase remains traceable from requirements to roadmap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User intent -> planning docs | A user can overstate or mis-scope the setup goal; the plan must preserve the documented scope rather than drift into unrelated tooling. |
| External tool claims -> repo documentation | The guide references third-party tools and install commands that the repo does not control; the plan must avoid implying ownership of those systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .planning/PROJECT.md and .planning/REQUIREMENTS.md | mitigate | Keep requirement IDs and out-of-scope boundaries explicit so the setup contract cannot silently expand. |
| T-01-02 | Information Disclosure | docs/best-setup-ever.md and planning docs | accept | The repository is intentionally a public documentation bundle; no sensitive data is stored in this phase. |
| T-01-03 | Repudiation | .planning/ROADMAP.md and .planning/STATE.md | mitigate | Record the current project reference and phase focus so future phases can trace decisions back to the roadmap. |
</threat_model>

<verification>
- Phase 1 docs must describe the project as a WSL-first AI setup and not as an application runtime.
- All active requirements in `REQUIREMENTS.md` must be mapped to roadmap phases.
- `ROADMAP.md` must expose a plan entry for Phase 1.
- `STATE.md` must reflect the current focus on defining the setup contract.
</verification>

<success_criteria>
- The project definition is stable and readable without consulting multiple files.
- The setup contract is expressed as traceable, user-facing requirements.
- Phase 1 is represented in the roadmap with at least one executable plan.
- No active requirement is left unmapped.
</success_criteria>

<output>
After completion, create `.planning/phases/01-define-the-setup-contract/01-01-SUMMARY.md`.
</output>
